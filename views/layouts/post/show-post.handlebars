<div class="row">
    <div class="col-md-8">
        <h1> {{title}} </h1>
        <hr>
        <small>Data de publicação: {{#formatDate date}}{{/formatDate}}</small>
        <hr>
        <p> {{content}} </p>
    </div>        
    <div class="col-md-4">
        {{>_side}}
    </div>
</div>